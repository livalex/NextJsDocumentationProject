// Error UI for a segment and its children
// Error boundaries display a fallback UI instead of the component tree that
// crashed.
"use client";

const error = () => {
  return <div>An error occurred</div>;
};

export default error;
